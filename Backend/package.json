{
  "name": "mern-todo-app",
  "private": true,
  "scripts": {
    "build": "npm install --prefix Server && npm install --prefix todolist && npm run build --prefix todolist",
    "start": "node Server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.3.0"
  }
}
